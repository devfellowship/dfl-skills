/**
 * Who a skill is by when its SKILL.md names no one, keyed by the repo owner.
 * The DFL registries are curated by Tainan, so an unattributed DFL skill is his.
 */
export const OWNER_DEFAULT_AUTHOR: Record<string, string> = Object.assign(Object.create(null), {
  devfellowship: "taigfs",
});
